<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.basketballscoretools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="篮球计分器"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.Home_StartScoretool" />
        <activity android:name=".ui.notifications.Notifications_AboutMe" />
        <activity android:name=".ui.notifications.Notifications_CommonQuestion" />
        <activity android:name=".ui.notifications.Notifications_important" />
        <activity android:name=".ui.notifications.Notifications_SendSuccess" />
        <activity android:name=".ui.notifications.Notifications_Suggestion" />
        <activity android:name=".ui.notifications.user.UserActivity" />
        <activity android:name=".ui.notifications.Login.RegisterActiviy"/>
        <activity android:name=".ui.notifications.Login.ModifierPasswordActivity"/>
    </application>

</manifest>